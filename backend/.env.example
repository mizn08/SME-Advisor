# Copy to `.env` in this folder (optional). Docker Compose sets these via `environment:`.

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=bnpl
POSTGRES_PASSWORD=bnpl_secret
POSTGRES_DB=bnpl_advisor
